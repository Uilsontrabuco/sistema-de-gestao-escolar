{
  "framework": null,
  "buildCommand": "",
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "excludeFiles": "{private,recovery,references,output,tmp,tests,supabase-port,.git,.local-build,.local-dependencies}/**"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index"
    }
  ]
}
